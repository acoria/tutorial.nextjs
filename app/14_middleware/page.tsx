import { middleware } from "@/middleware";
import { ReactElement } from "react";
/**
 * The middleware function {@link middleware} is used to intercept any request that is sent to the server.
 */
export default function Middleware(): ReactElement {
  return <>Middleware example</>;
}
