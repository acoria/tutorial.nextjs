/**
 * A layout file can be added to realize a global layout that is inherited by all children.
 * e.g. for central elements such as a navbar or a footer
 */
export default function SubPageInheritingLayout() {
  return <div>SubPage content embedded in global layout</div>;
}
